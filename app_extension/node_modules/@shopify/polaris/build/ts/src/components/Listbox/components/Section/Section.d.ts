import React from 'react';
import type { ReactNode } from 'react';
interface SectionProps {
    divider?: boolean;
    children?: ReactNode;
    title: ReactNode;
}
export declare function Section({ children, divider, title }: SectionProps): React.JSX.Element;
export {};
//# sourceMappingURL=Section.d.ts.map