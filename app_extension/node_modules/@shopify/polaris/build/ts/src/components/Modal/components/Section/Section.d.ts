import React from 'react';
export interface SectionProps {
    children?: React.ReactNode;
    flush?: boolean;
    subdued?: boolean;
    titleHidden?: boolean;
}
export declare function Section({ children, flush, subdued, titleHidden, }: SectionProps): React.JSX.Element;
//# sourceMappingURL=Section.d.ts.map