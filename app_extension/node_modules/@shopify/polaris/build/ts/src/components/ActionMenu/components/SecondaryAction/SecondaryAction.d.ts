import React from 'react';
import type { ButtonProps } from '../../../Button';
interface SecondaryAction extends ButtonProps {
    helpText?: React.ReactNode;
    destructive?: boolean;
    onAction?(): void;
    getOffsetWidth?(width: number): void;
}
export declare function SecondaryAction({ children, tone, helpText, onAction, getOffsetWidth, destructive, ...rest }: SecondaryAction): React.JSX.Element;
export {};
//# sourceMappingURL=SecondaryAction.d.ts.map