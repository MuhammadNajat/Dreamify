import { AppInterface } from '../models/app/app.js';
import { Config } from '@oclif/core';
interface VersionListOptions {
    app: AppInterface;
    apiKey?: string;
    reset: false;
    commandConfig: Config;
    json: boolean;
}
export default function versionList(options: VersionListOptions): Promise<void>;
export {};
