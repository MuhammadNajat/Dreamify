import { ExtensionInstance } from '../../models/extensions/extension-instance.js';
export declare function themeExtensionArgs(extension: ExtensionInstance, apiKey: string, token: string, options: {
    theme?: string;
    themeExtensionPort?: number;
    generateTmpTheme?: boolean;
    notify?: string;
}): Promise<string[]>;
