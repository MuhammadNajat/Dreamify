export declare const defaultQuery: string;
interface GraphiQLTemplateOptions {
    apiVersion: string;
    apiVersions: string[];
    appName: string;
    appUrl: string;
    randomKey: string;
    storeFqdn: string;
}
export declare function graphiqlTemplate({ apiVersion, apiVersions, appName, appUrl, randomKey, storeFqdn, }: GraphiQLTemplateOptions): string;
export {};
