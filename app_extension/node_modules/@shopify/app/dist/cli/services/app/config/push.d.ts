import { AppConfiguration } from '../../../models/app/app.js';
import { OutputMessage } from '@shopify/cli-kit/node/output';
export interface PushOptions {
    configuration: AppConfiguration;
    force: boolean;
}
export declare function pushConfig(options: PushOptions): Promise<void>;
export declare const abort: (errorMessage: OutputMessage) => never;
