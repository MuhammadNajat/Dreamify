import { AppConfigurationInterface } from '../../models/app/app.js';
export interface UpdateURLOptions {
    apiKey?: string;
    appURL?: string;
    redirectURLs?: string[];
    app: AppConfigurationInterface;
}
export default function updateURL(options: UpdateURLOptions): Promise<void>;
