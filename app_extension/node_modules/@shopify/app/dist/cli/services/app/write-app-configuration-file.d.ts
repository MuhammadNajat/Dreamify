import { AppConfiguration } from '../../models/app/app.js';
import { zod } from '@shopify/cli-kit/node/schema';
export declare function writeAppConfigurationFile(configuration: AppConfiguration): Promise<void>;
export declare const rewriteConfiguration: <T extends zod.ZodTypeAny>(schema: T, config: unknown) => unknown;
