import { AppConfiguration } from '../../../models/app/app.js';
import { OrganizationApp } from '../../../models/organization.js';
import { Config } from '@oclif/core';
export interface LinkOptions {
    commandConfig: Config;
    directory: string;
    apiKey?: string;
    configName?: string;
}
export default function link(options: LinkOptions, shouldRenderSuccess?: boolean): Promise<AppConfiguration>;
export declare function mergeAppConfiguration(appConfiguration: AppConfiguration, remoteApp: OrganizationApp): AppConfiguration;
