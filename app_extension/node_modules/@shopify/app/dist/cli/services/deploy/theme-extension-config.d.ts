import { ExtensionInstance } from '../../models/extensions/extension-instance.js';
export interface ThemeExtensionConfig {
    theme_extension: {
        files: {
            [key: string]: string;
        };
    };
}
export declare function themeExtensionConfig(themeExtension: ExtensionInstance): Promise<ThemeExtensionConfig>;
